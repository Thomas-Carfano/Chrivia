{
  "name": "chrivia",
  "version": "1.0.0",
  "description": "multiplayer trivia game",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "startAWS": "npm i && cd client && npm i && npm run build",
    "start": "npm i && cd client && npm i && npm run build && cd .. && cd server && node ./index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.6.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "gitignore": "^0.7.0",
    "morgan": "^1.10.0",
    "nodemon": "^3.0.2",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "prisma": "^5.6.0"
  }
}
